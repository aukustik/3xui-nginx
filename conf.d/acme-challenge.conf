# ACME Challenge for Let's Encrypt
server {
    listen 80;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        try_files $uri =404;
    }

    # Redirect other requests to HTTPS (uncomment after obtaining certificate)
    # location / {
    #     return 301 https://$host$request_uri;
    # }
}
