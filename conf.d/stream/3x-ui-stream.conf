# Stream module for TLS and XHTTP proxying
# Create this directory: mkdir -p conf.d/stream

# TLS proxy (uncomment for production)
# upstream xui_tls {
#     server 127.0.0.1:443;
# }
#
# server {
#     listen 443 ssl;
#     proxy_pass xui_tls;
#     proxy_timeout 1d;
#     proxy_connect_timeout 4s;
#
#     ssl_certificate /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
# }

# XHTTP proxy (uncomment for production)
# upstream xui_xhttp {
#     server 127.0.0.1:80;
# }
#
# server {
#     listen 80;
#     proxy_pass xui_xhttp;
#     proxy_timeout 1d;
#     proxy_connect_timeout 4s;
# }
